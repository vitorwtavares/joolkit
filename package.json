{
  "name": "noloop",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter client dev\" \"pnpm --filter server dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
